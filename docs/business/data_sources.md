# Источники данных

---

## Маршрутизация ИНИС / ИСНА

| Период | Источник |
|--------|---------|
| До 09.07.2025 | ИС **ИНИС** |
| С 09.07.2025 | ИС **ИСНА** |

Маршрутизация реализуется при ETL-импорте по дате записи (`completed_date` / `prescription_date`).

---

## Фильтр по управлению — только УНА

В КГД проверками занимаются несколько управлений: **УНА, УКН, УНН, УДМ, УАКН** и другие.

В расчёт KPI 1, 2, 3 входят **только проверки УНА** (Управление налогового аудита).

```python
# В ORM запросах всегда:
.filter(management='УНА')
```

> ⚠️ В реальной БД КГД явного поля `management` может не быть.
> УНА определяется через `re_user` → `department_id` → справочник подразделений.
> Уточнить с Алиханом при Спринте 18.

---

## Флаги учёта («признак учёта»)

Каждая исходная таблица содержит флаговый столбец — определяет включение записи в расчёт.

| Модель | Поле-флаг | Фильтр | Правило исключения |
|--------|-----------|--------|--------------------|
| `CompletedInspection` | `is_accepted` | `= True` | KPI 1, 2: после сверки данных ДГД с КГД |
| `CompletedInspection` | `is_counted` | `= True` | KPI 3: оператор вручную |
| `ActiveInspection` | `is_counted` | `= True` | KPI 5: исключить уголовные дела, запросы правоохранителей |
| `AppealDecision` | `is_counted` | `= True` | KPI 6: исключить акты >2 лет до решения комиссии; акты не-УНА |

**Кто проставляет флаги:**
- `CompletedInspection.is_accepted` — вручную после сверки данных ДГД с КГД
- `CompletedInspection.is_counted` — Олжас вручную для быстрого анализа
- `ActiveInspection.is_counted` — по правилам исключения
- `AppealDecision.is_counted` — по правилам исключения

> В системе: флаги проставляются оператором через UI (DataEditorView).

---

## ДФНО

**ДФНО** — дополнительные формы налоговой отчётности. Специальный тип проверок.

| KPI | ДФНО |
|-----|------|
| KPI 1 (доначисление) | ✅ включается |
| KPI 2 (взыскание) | ✅ включается |
| KPI 3 (среднее на проверку) | ❌ **исключается** |

```python
# Для KPI 3:
.filter(form_type != 'ДФНО')
```

---

## Правила исключения для KPI 5 (Проводимые)

Исключить из расчёта проверки:
- по уголовным делам
- по запросам правоохранительных органов

Поле: `ActiveInspection.case_type` — тип дела.

---

## Правила исключения для KPI 6 (Обжалования)

Исключить из расчёта:
- Акты, завершённые **более чем за 2 года** до даты решения Апелляционной комиссии
- Акты управлений **кроме УНА**

```python
# Правило 2 лет:
from datetime import timedelta
excluded = (decision_date - completion_date).days > 730
```

---

## Условные поля БД (заменить в Спринте 18)

### CompletedInspection — завершённые проверки
| Условное поле | Описание | Реальное поле |
|---------------|----------|--------------|
| `region_code` | Код ДГД | ⏳ уточнить |
| `management` | Управление (УНА/...) | ⏳ через re_user.department_id |
| `form_type` | Тип проверки (ДФНО/...) | ⏳ уточнить |
| `amount_assessed` | Сумма доначислений | ⏳ найдено примерно |
| `amount_collected` | Сумма взысканий | ⏳ найдено примерно |
| `is_accepted` | Флаг принято | ⏳ логический признак |
| `is_counted` | Флаг учёта | ⏳ логический признак |
| `completed_date` | Дата завершения | ⏳ даты расходятся в БД |

### ActiveInspection — проводимые проверки
| Условное поле | Описание | Реальное поле |
|---------------|----------|--------------|
| `region_code` | Код ДГД | ⏳ уточнить |
| `prescription_date` | Дата вручения предписания | ⏳ уточнить |
| `is_counted` | Флаг учёта | ⏳ логический признак |
| `case_type` | Тип дела | ⏳ уточнить |

### AppealDecision — обжалования
| Условное поле | Описание | Реальное поле |
|---------------|----------|--------------|
| `region_code` | Код ДГД | ⏳ Олжас добавит коды |
| `amount_cancelled` | Отменённая сумма | ⏳ уточнить |
| `completion_date` | Дата завершения акта | ⏳ уточнить |
| `decision_date` | Дата решения комиссии | ⏳ уточнить |

---

## Статус анализа БД КГД (апрель 2026)

Алихан (разработчик, ГПХ) проводит анализ. Текущий прогресс:
- ✅ Найдены три вьюхи: предписания, акт предписания, акт налоговой проверки
- ✅ Суммы доначислений найдены примерно
- ✅ `re_user.department_id` — путь к определению УНА
- ⏳ Корректные даты — расходятся в разных таблицах, ведётся поиск
- ⏳ Устойчивая связка: предписание → акт → суммы → даты → управление
- ⏳ CSV с полями и структурой — ожидается

**Цепочка связей (искомая):**
```
предписание → акт предписания → акт налоговой проверки → суммы → даты → department_id
```
